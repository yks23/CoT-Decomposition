
Given the following solution explanation, extract only the final answer as concisely as possible. Omit all steps, reasoning, or extra text. Provide just the number or expression.

Example usage:
Input:

```
As the problem suggests, we need to compute the lengths of the diagonals ... Thus, the answer is \frac 12 \cdot 10 \cdot 15 = \boxed{75}.
```

Output:

```
75
```
